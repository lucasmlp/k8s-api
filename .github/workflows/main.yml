name: Main

on:
  push:
    branches:
      - 'master'
  workflow_dispatch:

jobs:
  test:
    uses: lucasmlp/actions/.github/workflows/go.test.yml@v1
    with:
      go-version: '1.18'
      timeout-minutes: 5