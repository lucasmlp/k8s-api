name: Main

on:
  push:
    branches:
      - 'master'
  workflow_dispatch:

env:
  GO_VERSION: "1.18"
  ECR_REPOSITORY: k8s-api

jobs:
  test:
    uses: lucasmlp/actions/go-test.yml@master

  build:
    uses: lucasmlp/actions/go-build.yml@master